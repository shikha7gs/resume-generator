<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{name}}'s Resume</title>
  <style>
    body {
      font-family: 'Roboto', sans-serif;
      margin: 0;
      padding: 20px;
      background-color: #f4f7f6;
      color: #333;
    }

    .container {
      max-width: 800px;
      margin: 0 auto;
      background-color: #fff;
      padding: 30px;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .header {
      text-align: center;
      margin-bottom: 30px;
    }

    .header h1 {
      margin: 0;
      font-size: 3rem;
      color: #007bff;
    }

    .header p {
      font-size: 1.1rem;
      color: #555;
    }

    .header img {
      width: 120px;
      height: 120px;
      border-radius: 50%;
      margin-top: 20px;
    }

    .section {
      margin-bottom: 30px;
    }

    .section h2 {
      font-size: 1.8rem;
      margin-bottom: 15px;
      color: #007bff;
      border-bottom: 2px solid #007bff;
      padding-bottom: 5px;
    }

    .section p {
      margin: 8px 0;
      font-size: 1rem;
      color: #555;
    }

    .section ul.skills-list {
      list-style: none;
      padding: 0;
    }

    .skills-list li {
      display: inline-block;
      background-color: #f1f1f1;
      padding: 5px 10px;
      border-radius: 20px;
      margin: 5px 10px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <img src="{{imageUrl}}" alt="Profile Picture">
      <h1>{{name}}</h1>
      <p>{{contact}} | {{email}} | {{website}}</p>
      <p>{{address}}</p>
    </div>

    <div class="section">
      <h2>Summary</h2>
      <p>{{summary}}</p>
    </div>

    <div class="section">
      <h2>Education</h2>
      {{#each education}}
      <div>
        <p><strong>{{degree}}</strong> - {{institution}} ({{startYear}} - {{endYear}})</p>
        <p>{{details}}</p>
      </div>
      {{/each}}
    </div>

    <div class="section">
      <h2>Experience</h2>
      {{#each experience}}
      <div>
        <p><strong>{{position}}</strong> at {{company}} ({{startYear}} - {{endYear}})</p>
        <p>{{details}}</p>
      </div>
      {{/each}}
    </div>

    <div class="section">
      <h2>Skills</h2>
      <ul class="skills-list">
        {{#each skills}}
        <li>{{this}}</li>
        {{/each}}
      </ul>
    </div>
  </div>
</body>
</html>